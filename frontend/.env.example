# Recommended for VS Code dev tunnels (forward port 5173 only; backend runs on same machine as Vite):
VITE_API_URL=proxy

# Same computer, no tunnel:
# VITE_API_URL=http://localhost:8000

# Direct tunnel URLs (often hits CORS — prefer proxy instead):
# VITE_API_URL=auto
